#include <stdio.h>

int main(){
    int a, b, c;
    scanf("%d, %d, %d", &a, &b, &c); 
    int d = b * b - 4 * a * c;
    if (d>0){
        //-b+-d^(-2)/2ac
    }else if (d ==0){

    }else if (d < 0){

    }
}